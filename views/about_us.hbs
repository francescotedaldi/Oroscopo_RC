<h3>About us</h3>
<p>Francesco Tedaldi - Matricola: 1234567</p>
<p>Giovanni Milletti - Matricola: 1234567</p>
<p>Teodori Daniele - Matricola: 1657325</p>

<script>
    function debug(){
        var msg = "debug";
        alert(msg);
        window.location.href = "/dashboard"
    }
</script>

<script>
    const app = require('../app');
    const request = require('request');
    const express = require('express');
    const horoscope = require('../models/horoscope');


    var scegnoScelto = 'aquarius';
    var urlApi = process.env.URL_API + scegnoScelto;

    function ApiManager() {
        function getApi(error, response, body) {
            if (!error && response.statusCode == 200) {
                var jsonContent = JSON.parse(body);
                //console.log("\n\n!!! getApi: !!!\n\n");
                //console.log(jsonContent);
                var info_api = JSON.stringify(jsonContent);     //json to string
                //console.log("\n\n\n");
                alert(info_api);
            }
        }
        request.get(urlApi, getApi);
    }
</script>

<div class = "row">
    <input type="submit" value="Prova" class="btn" onclick="debug()">
    <input type="submit" value="Api" class="btn" onclick="ApiManager()">
</div>